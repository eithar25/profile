#include <iostream>

using namespace std;

int main()
{
   try{
    int father, son;
    cin>>father>>son;
    cout<<father,son;
    if (father>son){
        cout<<"these ages are possible";
    }
    }
   throw 404;
else(){
  catch (...)
  cout<<"son can't be older than father"
   }
}
